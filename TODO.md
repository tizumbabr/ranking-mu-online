- [ ] Corrigir o `lib/main.dart` (layout + lógica quebrada/indentação e duplicidades)
- [ ] Rodar `flutter analyze` para verificar erros
- [ ] Rodar `flutter run -d chrome` e confirmar que abre no Chrome sem erro
- [ ] (Depois) Ajustar ordenação/parse seguro do `reset`/`level` se ainda necessário

